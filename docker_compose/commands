
## to create container from docker compose

docker-compose -f docker_compose/docker-compose-nginx.yml  up -d
you can also can add -p in order to define name of project

## delete docker-compose
docker-compose -f docker_compose/docker-compose-nginx.yml downn